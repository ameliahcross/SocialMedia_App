@model SocialMedia_App.Core.Application.ViewModels.Login.LoginViewModel

@{
    ViewData["Title"] = "Login";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card p-3 bg-primary bg-opacity-10 mb-5">
                <div class="card-header bg-opacity-50 bg-primary">
                    <h3 class="text-center">@ViewData["Title"]</h3>
                </div>
                <div class="card-body">
                    @Html.ValidationSummary(false, " ", new {@class = "text-danger" })
                    <form asp-controller="User" asp-action="Index" method="post">
                        <div class="mb-3">
                            <label asp-for="Username" class="form-label">Username</label>
                            <input asp-for="Username" class="form-control">
                        </div>
                        <div class="mb-3">
                            <label asp-for="Password" class="form-label">Password</label>
                            <input asp-for="Password" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-primary">Iniciar sesión</button>
                        <a asp-controller="User" asp-action="Register" class="btn btn-secondary">Registrarme</a>
                        <a asp-controller="User" asp-action="Register" class="btn btn-info">Reestablecer contraseña</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>